export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-semibold">Sign In</h1>
      {/* TODO: Cognito sign-in form via @aws-amplify/auth */}
    </div>
  );
}
